<?xml version="1.0" encoding="UTF-8"?>
<job-scheduling-data
    xmlns="http://www.quartz-scheduler.org/xml/JobSchedulingData"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.quartz-scheduler.org/xml/JobSchedulingData 
        http://www.quartz-scheduler.org/xml/job_scheduling_data_1_8.xsd"
    version="1.8">
 
    <schedule>
        <job>
            <name>checkCalendarNotifications</name>
            <group>processGroup</group>
            <description>This process will run every 5 minutes to check for new calendar notifications to be sent out.</description>
            <job-class>com.rr.missouri.ui.jobs.checkCalendarNotifications</job-class>
        </job>
 
        <trigger>
            <cron>
                <name>60SecTrigger</name>
                <job-name>checkCalendarNotifications</job-name>
                <job-group>processGroup</job-group>
                <!-- seconds, minutes /(at) minute to start, hours, day of month, month, day of week, year (optional) -->
                <!-- Runs every minute-->
                <cron-expression>0 0/1 * * * ?</cron-expression>
            </cron>
        </trigger>
    </schedule>
    
    <schedule>
        <job>
            <name>checkForumNewNotifications</name>
            <group>processGroup</group>
            <description>This process will run every 30 seconds to check for new new post notifications to be sent out.</description>
            <job-class>com.rr.missouri.ui.jobs.checkForumNotifications</job-class>
        </job>
 
        <trigger>
            <cron>
                <name>newForumEmails</name>
                <job-name>checkForumNewNotifications</job-name>
                <job-group>processGroup</job-group>
                <!-- seconds, minutes /(at) minute to start, hours, day of month, month, day of week, year (optional) -->
                <!-- Runs every 45 seconds, starting at 3 seconds on the clock-->
                <cron-expression>45/3 * * * * ?</cron-expression>
            </cron>
        </trigger>
    </schedule>
    
</job-scheduling-data>
